
+diff --git a/src/catch2/catch_approx.cpp b/src/catch2/catch_approx.cpp
+index 08c6799b..ce360fc4 100644
+--- a/src/catch2/catch_approx.cpp
++++ b/src/catch2/catch_approx.cpp
+@@ -55,6 +55,22 @@ namespace Catch {
+             || marginComparison(m_value, other, m_epsilon * (m_scale + std::fabs(std::isinf(m_value)? 0 : m_value)));
+     }
+ 
++    bool Approx::lessThanOrEqualImpl(double lhs) const {
++        return lhs < m_value || equalityComparisonImpl(lhs);
++    }
++
++    bool Approx::lessThanOrEqualImplRight(double rhs) const {
++        return m_value < rhs || equalityComparisonImpl(rhs);
++    }
++
++    bool Approx::greaterThanOrEqualImpl(double lhs) const {
++        return lhs > m_value || equalityComparisonImpl(lhs);
++    }
++
++    bool Approx::greaterThanOrEqualImplRight(double rhs) const {
++        return m_value > rhs || equalityComparisonImpl(rhs);
++    }
++
+     void Approx::setMargin(double newMargin) {
+         CATCH_ENFORCE(newMargin >= 0,
+             "Invalid Approx::margin: " << newMargin << '.'
+diff --git a/src/catch2/catch_approx.hpp b/src/catch2/catch_approx.hpp
+index de4d2ab4..9a9a7f3e 100644
+--- a/src/catch2/catch_approx.hpp
++++ b/src/catch2/catch_approx.hpp
+@@ -21,6 +21,14 @@ namespace Catch {
+         void setMargin(double margin);
+         // Sets and validates the new epsilon (0 < epsilon < 1)
+         void setEpsilon(double epsilon);
++        // Helper for <= comparison with T on left
++        bool lessThanOrEqualImpl(double lhs) const;
++        // Helper for <= comparison with Approx on left
++        bool lessThanOrEqualImplRight(double rhs) const;
++        // Helper for >= comparison with T on left
++        bool greaterThanOrEqualImpl(double lhs) const;
++        // Helper for >= comparison with Approx on left
++        bool greaterThanOrEqualImplRight(double rhs) const;
+ 
+     public:
+         explicit Approx ( double value );
+@@ -66,22 +74,22 @@ namespace Catch {
+ 
+         template <typename T, typename = std::enable_if_t<std::is_constructible<double, T>::value>>
+         friend bool operator <= ( T const& lhs, Approx const& rhs ) {
+-            return static_cast<double>(lhs) < rhs.m_value || lhs == rhs;
++            return rhs.lessThanOrEqualImpl(static_cast<double>(lhs));
+         }
+ 
+         template <typename T, typename = std::enable_if_t<std::is_constructible<double, T>::value>>
+         friend bool operator <= ( Approx const& lhs, T const& rhs ) {
+-            return lhs.m_value < static_cast<double>(rhs) || lhs == rhs;
++            return lhs.lessThanOrEqualImplRight(static_cast<double>(rhs));
+         }
+ 
+         template <typename T, typename = std::enable_if_t<std::is_constructible<double, T>::value>>
+         friend bool operator >= ( T const& lhs, Approx const& rhs ) {
+-            return static_cast<double>(lhs) > rhs.m_value || lhs == rhs;
++            return rhs.greaterThanOrEqualImpl(static_cast<double>(lhs));
+         }
+ 
+         template <typename T, typename = std::enable_if_t<std::is_constructible<double, T>::value>>
+         friend bool operator >= ( Approx const& lhs, T const& rhs ) {
+-            return lhs.m_value > static_cast<double>(rhs) || lhs == rhs;
++            return lhs.greaterThanOrEqualImplRight(static_cast<double>(rhs));
+         }
+ 
+         template <typename T, typename = std::enable_if_t<std::is_constructible<double, T>::value>>
diff --git a/src/catch2/catch_approx.cpp b/src/catch2/catch_approx.cpp
index 08c6799b..d9d7ec65 100644
--- a/src/catch2/catch_approx.cpp
+++ b/src/catch2/catch_approx.cpp
@@ -20,6 +20,15 @@ bool marginComparison(double lhs, double rhs, double margin) {
     return (lhs + margin >= rhs) && (rhs + margin >= lhs);
 }
 
+double scaledMargin(double value, double scale, double epsilon) {
+    return epsilon * (scale + (std::isinf(value) ? 0.0 : std::fabs(value)));
+}
+
+bool approxEqual(double lhs, double rhs, double margin, double epsilon, double scale) {
+    return marginComparison(lhs, rhs, margin)
+        || marginComparison(lhs, rhs, scaledMargin(lhs, scale, epsilon));
+}
+
 }
 
 namespace Catch {
@@ -51,8 +60,7 @@ namespace Catch {
     bool Approx::equalityComparisonImpl(const double other) const {
         // First try with fixed margin, then compute margin based on epsilon, scale and Approx's value
         // Thanks to Richard Harris for his help refining the scaled margin value
-        return marginComparison(m_value, other, m_margin)
-            || marginComparison(m_value, other, m_epsilon * (m_scale + std::fabs(std::isinf(m_value)? 0 : m_value)));
+        return approxEqual(m_value, other, m_margin, m_epsilon, m_scale);
     }
 
     void Approx::setMargin(double newMargin) {
